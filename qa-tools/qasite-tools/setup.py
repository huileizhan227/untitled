from setuptools import setup

setup(
    name='qasite-tools',
    version='1.0.0',
    description='qasite tools',
    author='rinkky',
    packages=['qasite_tools'],
    install_requires=[
        'requests'
    ]
)
